import 'package:flutter/material.dart';

class CustomProgreesIndicator extends StatelessWidget {
  const CustomProgreesIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const 
    Center(child: CircularProgressIndicator(),);
  }
}